{
  "name": "relicworn-examples",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone, dependency-free extracts of patterns used in RELICWORN.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
